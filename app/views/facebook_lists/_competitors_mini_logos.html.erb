      <% if list.pages.any? %>
        <div class="user_avatars">
          <% list.pages.each do |competitor| %>
          	<%= link_to page_path(competitor.id), class: "no-decoration"  do %>
          		<%= image_tag page_get_picture(competitor.page_id), class: "mini_logo", alt: competitor.name, title: competitor.name %>
          	<% end %>
          <% end %>
        </div>
      <% end %>
