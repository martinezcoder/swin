<% provide(:title, "Evolución del engagement") %>

<% if not @error %>

	<%= render 'facebook/charts/charts' %>
	
	<%= render 'facebook/shared/graph', :graph => "graph_engage_evolution_div" %>
	<%= render 'facebook/shared/table' %>
	
<% else %>
		<strong><%= @error %></strong>
<% end %>
