<% provide(:title, @title) %>
<div class="row">
  <aside class="span4">
    <section>
		<h2>Página activa</h2>
		<%= render @page, :origin => nil %>
    </section>

    <section>    
    	<%= render 'change_page_link' %>
    </section>

    <section>
		<%= render 'competitors_mini_photos' %>
    </section>
  </aside>

  <div class="span8">
    <h3><%= @title %></h3>

	<%= link_to pages_search_path, class: "btn btn-danger", style: "margin-bottom: 20px;"  do %>
		<i class=" icon-search icon-white"></i> Buscar competidores
	<% end %>
  		
    <% if @competitors.any? %>
      <ul class="fb_pages">
		<% @competitors.each do |p| %>
			<li>
				<%= render p, :origin => 'competitors' %>
			</li>
		<% end %>
      </ul>
    <% end %>
  </div>
  
  
</div>




