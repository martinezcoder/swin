<% provide(:title, "Elija una página") %>

<div class="row">
  <aside class="span3">	
    <section>
		<h2>Página activa</h2>
		<%= render @page, :origin => ''  %>
    </section>

    <section>    
    	<%= render 'change_page_link' %>
    </section>
    
    <section>
		<%= render 'competitors_mini_photos' %>
    </section>
  </aside>

  <div class="span6">
	<h1>Estas son tus páginas de Facebook</h1>
	
	<h2>Elige una de tus páginas para monitorizar y comparar</h2>
	
	<ul class="fb_pages">
		<% @pages.each do |page| %>
		
			<li>
				<%= render page, :origin => 'index' %>

			 </li>
		<% end %>

	</ul>
  </div>
  
  <div class="span3" style="margin-top:160px;">
			<%= render 'index_update_list' %>
			<%= link_to competitors_user_page_path(@user, get_active_page), class: "btn btn-small btn-default"  do %>
				<i class=" icon-wrench"></i> Administrar competidores
			<% end %>


  </div>

</div>


